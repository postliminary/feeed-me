<ul class="nav nav-stacked">
  <% @feeds.each do |feed| %>
      <li>
        <%= link_to feed_path(feed.id) do %>
            <span><%= feed.title %></span>
        <% end %>
      </li>
  <% end %>
</ul>